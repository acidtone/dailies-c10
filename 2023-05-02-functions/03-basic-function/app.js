const name = 'Tony';

const name2 = 'Zee';

const greet = (name) => {
  const exclamation = 'Hi';
  return `${exclamation}!, ${name}`
}

console.log(greet(name));

console.log(greet(name2));

console.log(exclamation);