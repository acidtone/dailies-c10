// Refactor the following code into a pure function, squareNum, so that it:
// 1. Accepts a number as an argument;
// 2. Returns the square of that number.

const num = 4;
const numSquared = num * num;

console.log(numSquared);